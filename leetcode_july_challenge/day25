Find Minimum in Rotated Sorted Array II

#java solution

class Solution {
    public int findMin(int[] nums) {
        if(nums.length == 0) return -1;
        int min = nums[0];
        for(int i: nums){
            if(i < min) min = i;
        }
        return min;
    }
}